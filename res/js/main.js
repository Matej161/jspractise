/*
//!              parametry
const kulin = (item1, item2) => {
    console.log("Přinesl jsem " + item1 + " a " + item2);
}
kulin("michalovo bačkory", "myš")
//!              parametr
const kuulin = (item) => {
    console.log("Přinesl jsem " + item);
}
kuulin("nohy")


function sum(a, b, c, d, e, f) {
  console.log(a, b, c, d, e, f);
}
sum(1, 2, 3, 4, 5, 6);

const greetings = (name1, name2, name3, name4) => {
console.log ("Hello " + name1 + " Hello " + name2 + " Hello " + name3 + " Hello " + name4);
console.log (`Hello ${name1}, hello ${name2}, hello ${name3}, hello ${name4}`)
}
greetings("Pavle", "Radku", "Honzo", "Pepo");

let first = 5;
let second = 6;
let third = 4;

const sum = (a, b) => a + b;

let result = sum(first, second);
console.log(result);

function sumA(a, b) {
  return a + b;
}
let resultA = sumA(first, second);
console.log(resultA);

let resultB = sum(result, third);
console.log(resultB);

let person = "Jenda";
let age = 89;

function getSentence(a, b) {
  return `Tvoje jmeno je ${a} a je ti ${b} let`;
}

const createSentence = (a, b) => `tvoje jmeno je ${a} a je ti ${b} let`;

console.log(createSentence(person, age));

//TODO $ / shift + 4 (4 nad pismenem R)
*/
